# iOS client

SwiftUIとFoundationだけで構成するread-onlyの公式first-party API clientです。AI API、認証情報、write request、外部packageはありません。

前提はXcode 16以降、deployment targetはiOS 17です。`BiomedicalHydrogenEvidence.xcodeproj`を開き、開発teamを選択してsimulatorまたは実機で実行します。
